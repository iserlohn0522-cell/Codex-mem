interface:
  display_name: "Codex-mem"
  short_description: "Durable project memory with progressive Hellbender operations guidance."
  default_prompt: "Use $codex-mem to maintain canonical project memory. For a clearly operational University of Missouri Hellbender request, progressively load the Hellbender router, keep run evidence out of durable memory, and never treat recall as execution authorization. For cross-project maintenance, extract from project truth, deduplicate and forget only non-canonical support copies, then rebalance hot, deep, routing-only, and project-only priority. Keep retention human-controlled, never modify App-owned Native Memory, and treat OpenViking only as a recall hint."

policy:
  allow_implicit_invocation: true
